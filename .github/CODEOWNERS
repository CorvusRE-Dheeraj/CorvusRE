# Every file needs the owner's approval to merge into dev (enforced by the
# "Protect dev" ruleset, see .github/rulesets/protect-dev.json). Interns can
# open PRs and review each other's, but only a code owner's approval counts.
* @dheeraj-corvusrf
